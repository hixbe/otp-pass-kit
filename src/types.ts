export type Options = {
 lowerCaseAlphabets?: boolean;
 upperCaseAlphabets?: boolean;
 specialChars?: boolean;
 digits?: boolean;
 ensureEachType?: boolean; // Ensure at least one character from each enabled set
}